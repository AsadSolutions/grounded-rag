# When true, lib/api.ts serves all data from lib/mocks/ instead of the real backend.
NEXT_PUBLIC_USE_MOCKS=true

# Base URL of the FastAPI backend. Only used when NEXT_PUBLIC_USE_MOCKS is not "true".
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
